/**
 * POSPage — re-exports the existing POS implementation.
 * The POS module has complex internal state; it is migrated as-is
 * and will be progressively restyled in follow-up iterations.
 */
export { default } from '@/crm/pages/pos/index';
